(* this is a comment *)
(* this file is to test the EBNF interpreter, nothing else *)
program = { statement };
statement = { string | whitespace } , ";";
string = { letter | integer };
letter = "A" | "F" | "Q";
integer = non_0_digit, [ { any_digit } ];
any_digit = "0" | non_0_digit;
non_0_digit = "1" | "2" | "3" | "4" | "5" | "6" | "7" | "8" | "9";
whitespace = ? /\s/ ?;
