(* 
	This is an example EBNF file for testing reasons
*)
(*
	Lacking the patience for writing down 62 characters manually, the
	characters are defined in specials as regular expressions.
*)
whitespace = ? /\s+/ ?;
newline = ? /\n/ ?;
word = { ? /[a-zA-Z0-9_]/ ? };
args = ["-"], word;
command = word, [ { whitespace , args } ], newline ;
file = { command };
